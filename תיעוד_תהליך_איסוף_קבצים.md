# תיעוד תהליך איסוף קבצי דיגום ארובות
**תאריך:** 03/05/2026  
**מחבר:** TL (The Architect)  
**סביבה:** Cursor Cloud Agent + מחשב עבודה Windows

---

## 1. רקע ומטרה

### רקע
קיים מחשב עבודה ישן אשר מכיל קבצים הקשורים לתחום דיגום ארובות. הקבצים אינם מאורגנים, מפוזרים בתיקיות שונות על פני הדיסקים של המחשב. המחשב שייך / שימש דוברי עברית ורוסית.

### מטרה
לאתר את כל הקבצים הרלוונטיים הקשורים לדיגום ארובות (PDF, Word, Excel, תמונות, נהלים, טפסים, דוחות) ולרכז אותם לתיקייה אחת מסודרת, לצורך העברה לגיבוי חיצוני.

---

## 2. סביבת העבודה

### מחשב היעד (מחשב העבודה הישן)
- **מערכת הפעלה:** Windows (גרסה לא ידועה)
- **כתובת IP:** 192.168.1.108
- **כתובת IPv6:** 2a02:14f:1f5:3a2c:84c6:cc4e:fb22:cc32
- **ממשק רשת:** Ethernet (מחובר), Ethernet 2 (מנותק)
- **שפות המשתמשים:** עברית ורוסית

### סביבת שליטה (Cursor Cloud Agent)
- **מערכת הפעלה:** Ubuntu 24.04.4 LTS
- **גרעין:** Linux 6.1.147 x86_64
- **משתמש:** root
- **CPU:** Intel Xeon 4 ליבות
- **RAM:** 15GB
- **חיבור:** Cursor SSH Tunnel (russh) — אומת בהצלחה

---

## 3. תהליך העבודה — צעד אחר צעד

### שלב 1 — אימות חיבור

**מה נעשה:**  
נבדק מצב החיבור בין ה-Cloud Agent לסביבת העבודה על ידי הרצת פקודות מערכת בסיסיות: `whoami`, `hostname`, `uname -a`, `ifconfig`.

**מה התגלה:**  
ה-Cloud Agent רץ בתוך Container של Cursor ולא על מחשב העבודה הישן עצמו. הלוגים שסופקו בתחילת השיחה (`russh::server::kex`, `host relay client session successfully authed`) מתארים את חיבור ה-Tunnel של Cursor עצמו — לא חיבור למחשב העבודה.

**מסקנה:**  
נדרש לכתוב סקריפט שיופעל ישירות על מחשב Windows, ולא ניתן לבצע את הסריקה מרחוק דרך ה-Agent.

---

### שלב 2 — בחירת גישת העבודה

**אפשרויות שנשקלו:**
1. הרצת Cursor ישירות על מחשב העבודה
2. חיבור SSH למחשב העבודה (נדחה — לא סופקו פרטי גישה)
3. **כתיבת סקריפט PowerShell להרצה ידנית על מחשב העבודה** ← הגישה שנבחרה
4. כתיבת סקריפט PowerShell להרצה ידנית + העברה ידנית לגיבוי

**הנימוק לבחירה:**  
הדרך הפשוטה, הבטוחה והישירה ביותר. אין צורך בהגדרות רשת נוספות, אין תלות בתשתית חיצונית.

---

### שלב 3 — כתיבת סקריפט האיסוף הראשי

**קובץ:** `collect_files.ps1`  
**מיקום שמירה:** `/workspace/collect_files.ps1` (Cloud Agent) → הועתק ידנית ל-`C:\collect_files.ps1` על מחשב Windows

**פרמטרים שהוגדרו:**

| פרמטר | ערך |
|---|---|
| תיקיית פלט | `C:\COLLECTED_FILES\דיגום_ארובות_YYYY-MM-DD\` |
| דיסקים לסריקה | כל הדיסקים הזמינים (`Get-PSDrive`) |
| סיומות | pdf, doc, docx, xls, xlsx, ppt, pptx, txt, csv, xml, zip, rar, 7z, jpg, jpeg, png, tif, tiff, dwg, dxf |
| תיקיות מוחרגות | C:\Windows, C:\Program Files, C:\Program Files (x86), C:\ProgramData\Microsoft |

**מילות מפתח לסינון (עברית):**  
ארוב, ארובה, ארובות, דיגום, דגימ, פליטה, פליטות, נוהל, נהלים, טופס, טפסים, בדיקה, בדיקות, מדידה, מדידות, emission, stack, chimney, sampling, monitoring, protocol, procedure, form, report, דוח

**מבנה תיקיות פלט:**
```
C:\COLLECTED_FILES\דיגום_ארובות_YYYY-MM-DD\
├── PDF\
├── Word\
├── Excel\
├── תמונות\
├── שונות\
└── _סיכום_איסוף.txt
```

**לוגיקת טיפול בכפילויות:**  
אם קובץ עם שם זהה כבר קיים ביעד, מוסיף מספר רץ: `שם_קובץ_1.pdf`, `שם_קובץ_2.pdf` וכן הלאה.

---

### שלב 4 — ניסיון הרצה ושגיאה

**שגיאה שהתרחשה:**
```
-ExecutionPolicy : The term '-ExecutionPolicy' is not recognized...
```

**סיבת השגיאה:**  
המשתמש הריץ את הפקודה ללא המילה `PowerShell` בתחילתה, בתוך חלון PowerShell פתוח (לא כפקודה חיצונית).

**פתרון שנבחר:**  
במקום לדרוש שמירת קובץ והרצה חיצונית, הועתק הסקריפט כולו ישירות לתוך חלון PowerShell פתוח, עם הוספת שורה:
```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
```
בתחילתו לאפשר הרצה ללא הגבלת מדיניות.

---

### שלב 5 — הרצה מוצלחת

**תוצאה:** הסקריפט רץ בהצלחה. קבצים נאספו לתיקיית `C:\COLLECTED_FILES\`.  
**אינדיקטורים לסיום:** הופיע הפלט `=== סיום! ===` עם ספירת קבצים, ותיקיית הפלט נפתחה אוטומטית.

---

### שלב 6 — שאלות אימות ושיפור

לאחר הרצה מוצלחת עלו 5 שאלות מהמשתמש:

**שאלה 1 — האם זה כל הקבצים?**  
תשובה: לא בהכרח. הסקריפט מסנן לפי מילות מפתח בשם הקובץ/תיקייה. קובץ ללא שם רלוונטי לא ייאסף.  
פתרון: נוצר סקריפט אימות `1_verify_scan.ps1`.

**שאלה 2 — קבצים ברוסית?**  
תשובה: הסקריפט המקורי לא כלל מילות מפתח בקירילית. קבצים עם שמות/נתיבים ברוסית לא נאספו.  
פתרון: נוצר סקריפט השלמה `2_collect_russian.ps1` עם מילות מפתח ברוסית מהתחום.

**שאלה 3 — כל הדיסקים?**  
תשובה: כן. `Get-PSDrive -PSProvider FileSystem | Where-Object { $_.Used -gt 0 }` מחזיר את כל הדיסקים עם נתונים.

**שאלה 4 — לוג מסודר?**  
פתרון: נוצר סקריפט `3_generate_log.ps1` שיוצר קובץ תיעוד עם:
- סיכום כולל (ספירה + גודל)
- הפרדה לפי קטגוריה
- הפרדה לפי סוג קובץ
- רשימה אלפביתית מלאה עם גדלים ותאריכי שינוי

**שאלה 5 — תיעוד תהליך:**  
המסמך הנוכחי.

---

## 4. מילות מפתח ברוסית שנוספו

| קירילית | תרגום |
|---|---|
| труб | צינור / ארובה |
| дымо | ארובה / עשן |
| выброс | פליטה |
| эмисси | emission |
| загрязн | זיהום |
| замер | מדידה |
| измерени | מדידה / דגימה |
| отбор | איסוף דגימה |
| проб | דגימה |
| анализ | ניתוח |
| контрол | בקרה / ניטור |
| монитор | מוניטור |
| протокол | פרוטוקול / נוהל |
| процедур | נוהל |
| форм | טופס |
| отчет | דוח |
| акт | אישור / מסמך רשמי |
| журнал | יומן |
| экологи | אקולוגיה |
| охран | הגנה |
| атмосфер | אטמוספרה |

---

## 5. קבצים שנוצרו

| קובץ | מיקום | תפקיד |
|---|---|---|
| `collect_files.ps1` | `/workspace/` | סקריפט האיסוף הראשי |
| `1_verify_scan.ps1` | `/workspace/` | אימות — כמה קבצים על הדיסק vs שנאספו |
| `2_collect_russian.ps1` | `/workspace/` | השלמת איסוף קבצים ברוסית |
| `3_generate_log.ps1` | `/workspace/` | יצירת לוג מסודר ומאורגן |
| `תיעוד_תהליך_איסוף_קבצים.md` | `/workspace/` | המסמך הנוכחי |

---

## 6. סדר ההרצה המומלץ

```
1. הרץ: 1_verify_scan.ps1     ← בדוק כמה קבצים פספסת
2. הרץ: 2_collect_russian.ps1  ← אסוף קבצים ברוסית
3. הרץ: 3_generate_log.ps1     ← צור לוג מסודר מלא
4. בדוק: C:\COLLECTED_FILES\תיעוד_קבצים_מלא.txt
5. העבר: את תיקיית C:\COLLECTED_FILES לגיבוי חיצוני
```

---

## 7. הגבלות ידועות

- הסקריפט אינו אוסף קבצים שנמצאים בתיקיות מוחרגות (מערכת Windows).
- קבצים ללא מילת מפתח רלוונטית בשמם או בנתיב התיקייה שלהם לא ייאספו — זו החלטה מכוונת למנוע איסוף של עשרות אלפי קבצים לא רלוונטיים.
- אם יש קבצים שמוצפנים או נעולים על ידי תוכנה פעילה, הם יצוינו כשגיאה בלוג.
- גודל הדיסק הפנוי ב-`C:\` חייב להיות מספיק לאחסון כל הקבצים שנאספים.

---

## 8. שלבים הבאים

- [ ] הרצת `1_verify_scan.ps1` לאימות כמות הקבצים
- [ ] הרצת `2_collect_russian.ps1` להשלמת קבצים ברוסית
- [ ] הרצת `3_generate_log.ps1` לתיעוד מלא
- [ ] בדיקת הלוג ואימות שכל הקבצים הצפויים נמצאים
- [ ] העברת `C:\COLLECTED_FILES\` לדיסק גיבוי חיצוני
- [ ] אימות שהגיבוי הצליח (השוואת ספירת קבצים)

---

*מסמך זה נוצר אוטומטית על ידי Cursor Cloud Agent בתאריך 03/05/2026*
